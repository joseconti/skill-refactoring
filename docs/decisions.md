# Decisions — Refactor skill

Decisions are never re-opened on the assistant's initiative. A later explicit user request supersedes a recorded decision; record the change as a new entry.

- **D-0** (adopted as-is, 2026-07-16): The skill as released in v1.0.0 is the baseline — a role-neutral planning workflow (planning assistant + development AI), ten phases, references `code-map.md` / `golden-tests.md` / `dev-handoff.md`, all skill content written in English, version changes only on the user's explicit instruction. Adopted without retroactive re-litigation.
- **D-1** (2026-07-16): Version bump to **1.1.0** for the six-improvement sprint — explicit user instruction (option chosen via in-session question). Frontmatter, the Version section, the update-check reference and CHANGELOG.md updated together (they must always move together).
- **D-2** (2026-07-16): Keel state for this repo = **minimal** (PROGRESS.md, decisions.md, lessons-learned.md only) — explicit user choice. Full as-built reconstruction (01/02/03) and the gap audit (04) deliberately skipped; progressive backfill if ever needed.
- **D-3** (2026-07-16): The six v1.1.0 improvements, user-approved as a block ("arregla y mejora todo de la forma que mejor creas") after the assistant's honest assessment: (1) performance baseline + budget woven into Phases 1/3/6/7/8; (2) sprint-0 capture of fixtures and baseline from the reference version, hardened in every document that mentions capture; (3) Phase 8 review requires raw evidence, a contract-coverage table, fixtures' provenance, and a reviewer subagent by default; (4) mode-B migrations must be idempotent, resumable, batched, dry-run, with rollback and WP-CLI for big sites; (5) replacement strategy (full rewrite vs strangler) is an explicit Phase 3 decision recorded in ARCHITECTURE.md; (6) code map inventories are extracted mechanically first, with concrete sweep recipes in code-map.md.
- **D-4** (2026-07-16): The repo's Keel lock and embedded copy stay at **v1.11.0, untouched**. The Keel running in the Cowork session reports v3.0.0 but its local copy is incomplete (its SKILL.md names references that are absent), so syncing from it could embed a broken copy. José updates from his canonical Keel release when he chooses.
